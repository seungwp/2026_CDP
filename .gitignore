# colcon build artifacts
build/
install/
log/

# Python
__pycache__/
*.pyc
venv/
.venv/

# 런타임 로그 (hailort.log, pyhailort.log 등)
*.log

# 참고용 외부 문헌(특허공보/논문 PDF) — 제3자 저작물이라 추적하지 않는다.
# 인용은 docs/*.md 안에 문헌번호로만 남긴다.
docs/*.pdf

# graphify 지식 그래프 산출물 (로컬 분석용, 재생성 가능)
graphify-out/

# 모방학습 데이터·모델 (scripts/record_dataset.py, train_bc.py 산출물)
dataset/
*.onnx
